'use strict';
// Обектти се нещо, което в себе си съдържа други неща
// point, точката съдържа точните си координати в koordinatnata sistma

let point = {x: 5, y: -2};
console.log(point.x);
console.log(point.y);
console.log(typeof(point));

//The first element is the minimum age. The second and third elements are the name and age of the first person and the fourth and fifth elements – the name and age of the second person.

let filterByAge = {minAge: 12, name: 'Ivan', age: 15, name2:'Asen', age2:9};

console.log(filterByAge.min);



